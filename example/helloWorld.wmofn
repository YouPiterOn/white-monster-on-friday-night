var a = 20;

a = 60;

{
  const b = 123;
}

var b: []int = [1, 2, 3];

b = append(b, 4);

println(b[3], 3);

function toArray(vararg: int...): []int {
  return vararg;
}

b = toArray(1, 2, 3, 4, 5);

println(b);

function addToA(other: int): int {
  return (a + 0) + other;
}

function lessThenA(other: int): bool {
  return other < a;
}

addToA(5);

println(a);

1 + 1;

println(1);

1 + println(1);

if (lessThenA(69)) {
  return a;
} else {
  return addToA(42);
}
