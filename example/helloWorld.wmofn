var a = 20;

a = 60;

{
  const b = 123;
}

const b: int = 123;

function addToA(other: int): int {
  return (a + 0) + other;
}

function lessThenA(other: int): bool {
  return other < a;
}

1 + 1

println(1)

1 + println(1)

if (lessThenA(69)) {
  return a;
} else {
  return addToA(42);
}
